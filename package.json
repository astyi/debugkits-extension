{
  "name": "debugkits-api-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "watch": "node scripts/build.mjs --dev --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.295",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.3"
  }
}
